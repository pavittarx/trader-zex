from core.manifest import Manifest, Stage

MANIFEST = Manifest(
    name="continuation",
    stage=Stage.dropped,
    broker="fyers",
    notes="DROPPED 2026-06: real gross edge (+20%/yr) but break-even cost 8bps < 12-25bps real; limit-entry rescue tried, signal too thin",
)
