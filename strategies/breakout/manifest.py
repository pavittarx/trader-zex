from core.manifest import Manifest, Stage

MANIFEST = Manifest(
    name="breakout",
    stage=Stage.dropped,
    broker="fyers",
    notes="DROPPED 2026-06: gross ~ 0 — the compression-breakout premise is wrong here",
)
